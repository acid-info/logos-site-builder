import {ESiteTemplateNames} from "../types/data.types";
import {DefaultTemplate_Markdown} from "../templates/Deafult/DefaultTemplate";

export const logosTemplates = {
    [ESiteTemplateNames.DEFAULT]: DefaultTemplate_Markdown
};